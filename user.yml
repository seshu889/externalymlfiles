spring:
  application:
    name: user
    description : User related service
    version: 1.0-SNAPSHOT

 server:
   port: 4432

   protocol:
     name: dubbo
     port: 20880
     accesslog: dubbo-access.log
     provider:
       retries: 0
       timeout: 3000
     monitor:
       protocol: registry